import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import AppRouting from './AppRouting/AppRouting.js'

ReactDOM.render(
  <React.StrictMode>
    <AppRouting></AppRouting>
  </React.StrictMode>,
  document.getElementById('root')
);